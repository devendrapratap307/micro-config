global:
  message: welcome to order-service-dev
server:
  port: 8084
message: hello from order dev
spring:
  datasource:
    url: jdbc:h2:mem:order_master
    username: root
    password:
    # url: jdbc:mysql://localhost:3306/order_master
    # username: ${DB_USERNAME}
    # password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
