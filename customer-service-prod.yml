global:
  message: welcome to customer-service-prod
server:
  port: 8083
message: hello from prod
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cust_prrod
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
